// Gebruik het huidige repo-bestand voor deze file en pas alleen onderstaande casts toe als je handmatig merge
// 1) const copyResult = await copyWeld.mutateAsync(...) as Record<string, unknown>;
// 2) gebruik textOf(copyResult.weld_number || copyResult.id, "")
export {}

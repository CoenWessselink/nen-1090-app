// Gebruik het huidige repo-bestand en pas alleen deze twee regels toe:
// const copyResult = await copyWeld.mutateAsync(...) as Record<string, unknown>;
// onMessage(`Las ... als ${String(copyResult.weld_number || copyResult.id || "")}.`)
export {}

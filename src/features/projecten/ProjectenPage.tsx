/**
 * Legacy / unused in `createBrowserRouter` config; aligned with mobile projects UI.
 */
import { MobileProjectsPage as ProjectenPage } from '@/features/mobile/MobileProjectsPage';

export { ProjectenPage };
export default ProjectenPage;

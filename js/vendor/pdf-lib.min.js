/*! pdf-lib stub (offline placeholder) */
(function(global){
  if(global.PDFLib) return;
  function _err(){ throw new Error("pdf-lib placeholder: voeg echte pdf-lib.min.js toe om PDF merge/print te activeren."); }
  global.PDFLib = {
    PDFDocument: {
      create: async function(){ _err(); },
      load: async function(){ _err(); }
    },
    rgb: function(){ _err(); },
    StandardFonts: {},
  };
})(window);

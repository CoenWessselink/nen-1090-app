# Phase 2 billing merge completed

- React app billing route is now a central handoff page instead of a second management UI.
- Users are redirected to the marketing subscription shell for plan, seats, invoices and checkout.
- Tenant billing status remains visible in the app for context.
